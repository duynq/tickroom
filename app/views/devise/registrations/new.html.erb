<div id="sign-up">
  <h1>Đăng ký tài khoản</h1>
  <div class="clearfix">
    <div id="sign-up-form">
      <%= form_for(resource, as: resource_name,
        url: registration_path(resource_name)) do |f| %>
        <%= devise_error_messages! %>

        <div class="field">
          <%= f.label :Name, "User name" %><br />
          <%= f.text_field :name, autofocus: true, class: "form-control" %>
        </div>
       <div class="field">
          <%= f.label :email %><br />
          <%= f.email_field :email, class: "form-control" %>
        </div>
        <div class="field">
          <%= f.label :password %>
          <% if @minimum_password_length %>
          <em>(ít nhất 8 kí tự)</em>
          <% end %><br />
          <%= f.password_field :password, autocomplete: "off", class: "form-control" %>
        </div>

        <div class="field">
          <%= f.label :password_confirmation %><br />
          <%= f.password_field :password_confirmation, autocomplete: "off", class: "form-control" %>
        </div>

        <div class="actions">
          <%= f.submit "Đăng ký", class: "signup-button btn-submit" %>
        </div>
      <% end %>
      <%= render "devise/shared/links" %>
    </div>
    <div id="sign-up-left">
      <h2> Đăng kí tài khoản để trở thành thành viên của trang web </h2>
      <p> Khi trở thành thành viên của trang web, bạn có thể thuê phòng hơn thế nữa bạn có thể trở thành chủ phòng và kiếm được tiền từ việc cho thuê phòng.</p>
      <center>
        <%= image_tag "dollar.png" %>
      </center>
    </div>
  </div>
</div>
